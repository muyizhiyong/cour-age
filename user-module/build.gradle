project('user-api') {
    dependencies {

    }
}
project('user-impl') {
    dependencies {
        // 依赖common
        implementation project(':common')
        implementation project(':user-module:user-api')

    }
}
